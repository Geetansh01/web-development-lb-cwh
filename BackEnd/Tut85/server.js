// const slugify = require("./node_modules/slugify"); //Also works
const slugify = require("slugify");

console.log('\nYAY! You ran JS outside the browser');
console.log('This is like a server written in JS, except it does not keep running!');
console.log('we will write an actual server like this in future');

console.log(slugify("This has Spaces but slugify will remove them!"));

console.log('\n');





